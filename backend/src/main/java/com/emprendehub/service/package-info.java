/**
 * Lógica de negocio. Clases {@code @Service}.
 *
 * <p>Es la capa donde viven las reglas del dominio documentadas en
 * {@code docs/decisiones-dominio.md} y <strong>donde se mide el 80% de
 * cobertura</strong> que exige la rúbrica.
 *
 * <p>Se prueba con Mockito sustituyendo el repositorio, sin base de datos.
 * Recibe y devuelve records de {@code dto}; las entidades de {@code model} no
 * salen de aquí.
 */
package com.emprendehub.service;
